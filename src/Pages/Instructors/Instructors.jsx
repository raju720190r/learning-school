import { Helmet } from "react-helmet";
import { useQuery } from "react-query";


const Instructors = () => {
    
    return (
        <div>
            <Helmet><title>Learning School || Instructors</title></Helmet>
            <h2 className="text-5xl">Instructors</h2>
            
        </div>
    );
};

export default Instructors;